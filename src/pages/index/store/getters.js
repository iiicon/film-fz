export const currentCity = state => state.currentCity

export const currentShow = state => state.currentShow

export const currentId = state => state.currentId

export const totalPrice = state => state.totalPrice

export const ticketCount = state => state.ticketCount

export const expressAddress = state => state.expressAddress

export const showItemUnitId = state => state.showItemUnitId

export const orderNo = state => state.orderNo
